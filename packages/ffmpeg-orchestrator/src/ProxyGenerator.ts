import { MediaInfo } from './FFmpegOrchestrator';

export class ProxyGenerator {
  // Cette classe pourrait être étendue pour gérer la génération de proxies
  // Pour l'instant, la logique est dans FFmpegOrchestrator
}
